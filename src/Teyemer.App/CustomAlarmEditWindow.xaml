<Window x:Class="Teyemer.App.CustomAlarmEditWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:Teyemer.App"
 Title="사용자 알람 설정" Width="520" Height="430" MinWidth="480" MinHeight="400"
 WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
 Background="Transparent" UseLayoutRounding="True" SnapsToDevicePixels="True">
 <Border Margin="10" Padding="24" Background="{DynamicResource GlassSurfaceBrush}"
  BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="16">
  <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <StackPanel>
    <TextBlock Text="사용자 알람 설정" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,18"/>
    <Grid Style="{StaticResource FormRowStyle}"><Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <TextBlock Text="사용 여부" Style="{StaticResource FormLabelStyle}"/>
     <CheckBox Grid.Column="1" Content="이 알람 사용" Margin="0,8" IsChecked="{Binding IsEnabled, Mode=TwoWay}"/>
    </Grid>
    <Grid Style="{StaticResource FormRowStyle}"><Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <TextBlock Text="유형" Style="{StaticResource FormLabelStyle}"/>
     <local:AlarmTypePicker Grid.Column="1" SelectedType="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
    <Grid Style="{StaticResource FormRowStyle}"><Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <TextBlock x:Name="ScheduleTitle" Text="주기" Style="{StaticResource FormLabelStyle}"/>
     <StackPanel x:Name="PeriodicEditor" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><TextBox Width="96" Margin="0" Text="{Binding IntervalMinutes, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="분마다" Margin="10,0,0,0" VerticalAlignment="Center" Style="{StaticResource HintTextStyle}"/></StackPanel>
     <local:TimePicker x:Name="DailyEditor" Grid.Column="1" SelectedTime="{Binding Time, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed"/>
    </Grid>
    <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
     <TextBlock Text="표시 문구" VerticalAlignment="Top" Margin="0,10,16,0"/>
     <TextBox Grid.Column="1" MinHeight="80" Margin="0" MaxLength="200" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
    <TextBlock x:Name="ValidationMessage" Foreground="{DynamicResource ErrorBrush}" Margin="140,10,0,0" TextWrapping="Wrap"/>
   </StackPanel>
   <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0"><Button Content="취소" Style="{StaticResource ActionButtonStyle}" Click="Cancel_Click"/><Button Content="저장" Style="{StaticResource PrimaryActionButtonStyle}" IsDefault="True" Click="Save_Click"/></StackPanel>
  </Grid>
 </Border>
</Window>
